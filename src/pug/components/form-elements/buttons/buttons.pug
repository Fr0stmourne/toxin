mixin button(options)
  case options.type
    when 'secondary'
      - var btnClass = 'btn btn--secondary'
    default
      - var btnClass = 'btn btn--primary'
  button(class=btnClass)=options.btnText
