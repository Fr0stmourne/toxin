mixin button(options = {})
  -
    var {
      styleType="",
      type="submit",
      buttonText="Кнопка",
      link="",
      slim=false
    } = options
  case type
    when "button"
      - var type = "button"
    when "reset"
      - var type = "reset"
    default
      - var type = "submit"
  case styleType
    when "secondary"
      - var buttonClass = "button_type_secondary"
    when "proceed"
      - var buttonClass = "button_type_proceed"
    default
      - var buttonClass = "button_type_primary"
  if link
    a(href=link class=`button ${buttonClass} ${slim ? "button_size_slim" : "" }`)=buttonText
  else
    button(class=`button ${buttonClass} ${slim ? "button_size_slim" : "" }` type=type)=buttonText
