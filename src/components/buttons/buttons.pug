mixin button({type = '', btnText = 'Кнопка', link = ''} = {})
  case type
    when 'secondary'
      - var btnClass = 'btn btn_secondary'
    when 'proceed'
      - var btnClass = 'btn btn_proceed'
    default
      - var btnClass = 'btn btn_primary'
  if link
    a(href=link class=btnClass)=btnText
  else
    button(class=btnClass)=btnText
