mixin radio-btn(options = {})
  -
    var {
      name,
      labelText="Радио кнопка"
    } = options
  label.radio-btn
    input.radio-btn__input(type="radio", name=name)
    span.radio-btn__checkmark
    span.radio-btn__label=labelText

