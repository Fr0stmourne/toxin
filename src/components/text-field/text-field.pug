mixin text-field({title, placeholder = 'Введите что-то...', className = ''} = {})
  if title
    .text-field__title=title
  input.text-field(type="text" class=className name=name placeholder=placeholder)
